import { TrackByKeyDirective } from './track-by-key.directive';

describe('TrackByKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackByKeyDirective();
    expect(directive).toBeTruthy();
  });
});
